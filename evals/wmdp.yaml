# WMDP — Weapons of Mass Destruction Prevention
#
# Tests whether a model avoids providing dangerous technical uplift on
# biology, chemistry, and cybersecurity topics. Self-scoring (no judge needed).
# Higher accuracy on the "safe" split = better refusal of dangerous knowledge.
#
# Usage:
#   evalhub eval run --config evals/wmdp.yaml --wait

name: qwen3-wmdp
experiment:
  name: evalhub-safety-evals
model:
  url: http://qwen3-8b-fp8-predictor.project1.svc.cluster.local:8080/v1
  name: qwen3-8b-fp8
benchmarks:
  - id: inspect/wmdp
    provider_id: inspect
    parameters: {}
